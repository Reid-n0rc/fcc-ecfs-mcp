---
description: Search FCC ECFS filings by docket, filer name, submission type, date range, or free text.
---

Call `mcp__fcc-ecfs__ecfs_search_filings` to search filings for: $ARGUMENTS

Map any docket number to `proceedings_name`, commenter/filer name to `filers_name`, and keyword text to `q`. Present the results as a concise list (filer, date, submission type, docket) rather than raw JSON.
